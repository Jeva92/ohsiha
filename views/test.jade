extends ../node_modules/jade-bootstrap/_bootstrap.jade
block body
    +navbar("test", "dropdown_menu")
        +nav_item("/") Index
        +nav_item("/test") Test
    .container
        table.table.table-bordered
            tbody
                each t, i in json.items
                    tr
                        td
                            a(href=t.url) #{t.title}
                    tr
                        td 
                            #{t.description}
                    tr
                        td= "Tähän voisi vaikka kommentoinnin tms"
                     - if (i == 6)
                         - break;